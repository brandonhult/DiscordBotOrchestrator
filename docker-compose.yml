version: "3.9"

services:
  discordbotorchestrator:
    build: .
    container_name: discordbotorchestrator
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    logging:
      options:
        max-size: "10m"
        max-file: "3"